<div class="page-header">
  <%= link_to project_videos_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to @video, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>NO HAY EDICION DEL VIDEO! SOLO SE BORRA Y LOS NUEVOS SNAPSHOTS SALEN DESDE EL VIEW!</h1>
</div>

<%# render 'form' %>
